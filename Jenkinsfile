pipeline {
    agent any
    stages {
        stage ("code") {
            steps {
                echo "this is for code"
            }
        }
        stage ("build+test") {
            steps{
                sh "this is for build code"
            }
        }

