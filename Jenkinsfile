pipeline {
    agent any
    stages {
        stage ("code") {
            steps {
                echo "this is for code"
            }
        }
        stage ("build+test") {
            steps{
                 echo "this is for build code"
            }
        }

